{
  "name": "cors-anywhere-server",
  "version": "1.0.0",
  "description": "Proxy requests so canvas can think it is safe",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.3"
  },
  "author": "",
  "license": "MIT"
}
